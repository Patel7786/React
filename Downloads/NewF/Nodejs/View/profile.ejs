<!DOCTYPE html>
<html lang="en">
  <head>
    <title>
      Node Js MySQL Fetch and Show Records from MySQL Database Example
    </title>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
  </head>
  <body>
    <div class="card " style="background-color: aqua;">
        <div class="card-header text-end">
            <h5 class="card-title text-center">
                Result Management System
                <a href="/logout" class="btn btn-primary badge bg-light text-dark bg-primary float-right">Logout</a>
            </h5>
        </div>
    </div>
    <br>
    <h4>
        <a href="/" class="btn btn-primary badge bg-light text-dark bg-primary float-right">Back</a>
        <a href="/addrecord" class="btn btn-primary badge bg-light text-dark bg-primary float-right">Add Record</a>
    </h4>

    <div class="container mt-4">
      
      <br />
      <table class="table">
        <thead>
          <tr>
            <th>#Id</th>
            <th>Roll Number</th>
            <th>Name</th>
            <th>Score</th>
            <th>Date of Birth</th>
            <th>Action</th>
          </tr>
        </thead>
        <tbody>
          <% if(data.length){ for(var i = 0; i< data.length; i++) {%>
          <tr>
            <th scope="row"><%= (i+1) %></th>
            <td><%= data[i].RollNumber%></td>
            <td><%= data[i].Name%></td>
            <td><%= data[i].Score%></td>
            <td type="date" placeholder="gg/mm/aaaa"><%= data[i].DOB%></td>
            <td><a href="/edit/<%=data[i].RollNumber%>">Edit</a></td>
            <td><a href="/delete/<%=data[i].RollNumber%>">Delete</a></td>
          </tr>
          <% } }else{ %>
          <tr>
            <td>No data ever existed.</td>
          </tr>
          <% } %>
        </tbody>
      </table>
    </div>
  </body>
</html>